[package]
name = "rustcraft-server"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Authoritative game server: lazy infinite world, deltas, surface caches, physics (wasm-safe)"

[dependencies]
rustcraft-world = { path = "../rustcraft-world" }
